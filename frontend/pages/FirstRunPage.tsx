import styled from "@emotion/styled";
import React, { JSXElementConstructor, MouseEvent, ReactElement } from "react";

interface Props {}

const FirstRunPage = (props: Props) => {
  return (
    <></>
  );
};

export default FirstRunPage;